import { defineMessages } from 'react-intl';

export default () => ({
  panel: defineMessages({
    manualCount: {
      id: 'panel.ext.ucbnh-loanout.manualCount',
      defaultMessage: 'Manual Counts',
    },
  }),
});
